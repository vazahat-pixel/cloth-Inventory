/**
 * pdf.generator.js — PDF generation stub (use pdfkit or puppeteer)
 */

// TODO: Install pdfkit: npm install pdfkit
// const PDFDocument = require('pdfkit');

const generateInvoicePDF = async (invoiceData) => {
    // Stub — implement with pdfkit or puppeteer
    throw new Error('PDF generation not yet implemented. Install pdfkit to enable.');
};

module.exports = { generateInvoicePDF };
