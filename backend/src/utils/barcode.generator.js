/**
 * barcode.generator.js — Barcode generation stub
 */

// TODO: Install bwip-js: npm install bwip-js
// const bwipjs = require('bwip-js');

const generateBarcode = async (text, format = 'code128') => {
    // Stub — returns placeholder
    // In production, use bwipjs.toBuffer({ bcid: format, text, ... })
    throw new Error('Barcode generation not yet implemented. Install bwip-js to enable.');
};

module.exports = { generateBarcode };
